export enum Role {
    Student = 'student',
    Coach = 'coach',
    Admin = 'admin'
}

export enum RequestStatus {
    Pending = 'pending',
    Accepted = 'accepted',
    Rejected = 'rejected',
    Canceled = 'canceled'
}

export enum Level {
    Beginner = 'beginner',
    Advanced = 'advanced',
    Veteran = 'veteran',
    Expert = 'expert'
}